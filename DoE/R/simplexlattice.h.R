
# This file is automatically generated, you probably don't want to edit this

simplexLatticeOptions <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "simplexLatticeOptions",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            fac = NULL,
            lev = NULL,
            designPoints = FALSE, ...) {

            super$initialize(
                package="DoE",
                name="simplexLattice",
                requiresData=TRUE,
                ...)

            private$..fac <- jmvcore::OptionNumber$new(
                "fac",
                fac)
            private$..lev <- jmvcore::OptionNumber$new(
                "lev",
                lev)
            private$..designPoints <- jmvcore::OptionBool$new(
                "designPoints",
                designPoints,
                default=FALSE)

            self$.addOption(private$..fac)
            self$.addOption(private$..lev)
            self$.addOption(private$..designPoints)
        }),
    active = list(
        fac = function() private$..fac$value,
        lev = function() private$..lev$value,
        designPoints = function() private$..designPoints$value),
    private = list(
        ..fac = NA,
        ..lev = NA,
        ..designPoints = NA)
)

simplexLatticeResults <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "simplexLatticeResults",
    inherit = jmvcore::Group,
    active = list(
        usage = function() private$.items[["usage"]],
        text = function() private$.items[["text"]],
        designPointsPlot = function() private$.items[["designPointsPlot"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="Simplex Lattice")
            self$add(jmvcore::Html$new(
                options=options,
                name="usage",
                title="Usage",
                visible=TRUE))
            self$add(jmvcore::Preformatted$new(
                options=options,
                name="text",
                title="Simplex Lattice"))
            self$add(jmvcore::Image$new(
                options=options,
                name="designPointsPlot",
                title="Design Points Plot",
                width=350,
                height=350,
                renderFun=".designPointsPlot"))}))

simplexLatticeBase <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "simplexLatticeBase",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = "DoE",
                name = "simplexLattice",
                version = c(1,0,0),
                options = options,
                results = simplexLatticeResults$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE,
                requiresMissings = FALSE)
        }))

#' Simplex Lattice
#'
#' 
#' @param data .
#' @param fac .
#' @param lev .
#' @param designPoints .
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$usage} \tab \tab \tab \tab \tab a html \cr
#'   \code{results$text} \tab \tab \tab \tab \tab a preformatted \cr
#'   \code{results$designPointsPlot} \tab \tab \tab \tab \tab an image \cr
#' }
#'
#' @export
simplexLattice <- function(
    data,
    fac,
    lev,
    designPoints = FALSE) {

    if ( ! requireNamespace("jmvcore", quietly=TRUE))
        stop("simplexLattice requires jmvcore to be installed (restart may be required)")

    if (missing(data))
        data <- jmvcore::marshalData(
            parent.frame())


    options <- simplexLatticeOptions$new(
        fac = fac,
        lev = lev,
        designPoints = designPoints)

    analysis <- simplexLatticeClass$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}

